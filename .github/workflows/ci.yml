name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    # Pinned to v1 (latest stable v1.x.y).
    uses: nos-tromo/.github/.github/workflows/python-app-ci.yml@v1
    with:
      python-versions: '["3.11","3.12","3.13"]'
      uv-sync-args: '--frozen'
      docker-build: true
